---
title: 认证源协议选型指南
description: 本文档帮助您根据业务场景选择合适的认证协议。专属集成平台支持多种认证协议，包括OIDC、OAuth2、SAML、CAS以及多种社交登录方式。
---

## 概述

本文档帮助您根据业务场景选择合适的认证协议。专属集成平台支持多种认证协议，包括OIDC、OAuth2、SAML、CAS以及多种社交登录方式。

## 协议对比概览

| 协议 | 复杂度 | 安全性 | 适用场景 | 推荐度 |
|------|--------|--------|----------|--------|
| **OIDC** | 中 | 高 | 现代Web应用、移动端 | ⭐⭐⭐⭐⭐ |
| **OAuth2** | 低 | 中 | API授权、第三方登录 | ⭐⭐⭐⭐ |
| **SAML** | 高 | 高 | 企业级应用、SSO | ⭐⭐⭐⭐ |
| **CAS** | 中 | 中 | 单点登录、老系统 | ⭐⭐⭐ |

## 协议详解

### OIDC (OpenID Connect)

**简介**：OIDC是基于OAuth2.0的身份层协议，是现代应用推荐的身份认证标准。

**优势**：
- 基于JSON，轻量级易于实现
- 内置身份令牌（ID Token），包含丰富用户信息
- 支持RESTful API，易于与移动端集成
- 良好的移动端支持
- 标准化程度高，互操作性好

**适用场景**：
- 现代Web应用（SPA、移动端）
- 需要获取用户详细信息的应用
- 微服务架构
- 多平台统一身份认证

**不适用场景**：
- 老旧系统（不支持OAuth2）
- 对协议复杂性有严格限制的环境

**配置要点**：
- 选择合适的授权类型（Authorization Code推荐）
- 配置正确的回调URL
- 根据需求选择授权范围（Scope）

### OAuth 2.0

**简介**：OAuth2.0是授权协议的标准，主要用于API授权而非身份认证。

**优势**：
- 成熟的授权机制
- 广泛的平台支持
- 灵活的作用域控制
- 适合资源访问授权

**适用场景**：
- 第三方应用授权（如"使用微信登录"）
- API访问授权
- 跨系统数据共享

**不适用场景**：
- 需要严格身份认证的场景（应使用OIDC）
- 需要获取详细用户信息的场景

**注意**：如果只需要用户身份认证，强烈建议使用OIDC而非纯OAuth2。

### SAML 2.0

**简介**：SAML是XML格式的企业级SSO标准，广泛用于企业应用集成。

**优势**：
- 企业级安全标准
- 成熟的SSO实施经验
- 广泛的企业应用支持
- 无需直接传递密码

**适用场景**：
- 企业内部应用SSO
- 需要与AD、LDAP集成的场景
- 大型企业组织
- 对安全性要求高的场景

**不适用场景**：
- 移动端应用（推荐OIDC）
- 微服务架构
- 简单的第三方登录

**配置要点**：
- 正确配置Entity ID和ACS URL
- 确保时钟同步
- 配置正确的属性映射

### CAS (Central Authentication Service)

**简介**：CAS是专门为Web应用设计的单点登录协议。

**优势**：
- 专为SSO设计
- 简单易用
- 良好的会话管理
- 支持代理认证

**适用场景**：
- 传统Web应用
- 需要简单SSO的场景
- 需要代理认证的场景
- 教育机构（很多教育系统使用CAS）

**不适用场景**：
- 移动端应用
- 需要与现代协议兼容的场景

## 协议选型决策树

```
开始
  │
  ├─ 是否需要获取用户详细信息？
  │   │
  │   ├─ 是 → 是否是现代Web/移动应用？
  │   │   │
  │   │   ├─ 是 → 推荐 OIDC
  │   │   │
  │   │   └─ 否 → 是否是企业级应用？
  │   │       │
  │   │       ├─ 是 → 推荐 SAML
  │   │       │
  │   │       └─ 否 → 推荐 CAS
  │   │
  │   └─ 否 → 只需要授权还是需要认证？
  │       │
  │       ├─ 仅授权 → 推荐 OAuth2
  │       │
  │       └─ 需要认证 → 推荐 OIDC
  │
  └─ 是否有现有AD/LDAP？
      │
      ├─ 是 → 推荐 SAML + AD集成
      │
      └─ 否 → 推荐 OIDC
```

## 企业微信/钉钉/飞书选型

对于国内企业办公场景的特殊平台：

| 平台 | 推荐协议 | 说明 |
|------|---------|------|
| **企业微信** | OAuth2 + 自定义 | 使用企业微信OAuth2接口 |
| **钉钉** | OIDC | 钉钉提供OIDC支持 |
| **飞书** | OIDC | 飞书提供OIDC支持 |

### 选择建议

- **企业微信**：如果只需要扫码登录，使用OAuth2；如果需要完整用户信息，使用OIDC
- **钉钉**：推荐使用OIDC协议，获取完整的用户信息
- **飞书**：推荐使用OIDC协议，与飞书账号体系无缝集成

## 常见问题

### Q1: OIDC和OAuth2有什么区别？

**A**: 简单来说，OIDC = OAuth2 + 身份标识。OAuth2只授权访问资源，OIDC在此基础上提供用户身份信息。如果需要确认用户身份，应使用OIDC而非纯OAuth2。

### Q2: SAML太复杂，可以换成OIDC吗？

**A**: 如果应用支持，两种协议可以共存。对于新开发的应用，推荐使用OIDC；对于已有SAML集成的应用，建议保持SAML或逐步迁移。

### Q3: 移动端推荐使用什么协议？

**A**: 推荐使用OIDC的Authorization Code + PKCE流程，这是移动端最安全的认证方式。

### Q4: 如何保证认证安全性？

**A**: 几点建议：
- 生产环境务必使用HTTPS
- 选择安全的授权类型（避免Implicit）
- 配置适当的令牌有效期
- 启用令牌刷新机制
- 敏感操作要求重新认证

## 相关文档

- [认证源-OIDC协议](./认证源-OIDC协议.md)
- [认证源-OAuth2协议](./认证源-OAuth2协议.md)
- [认证源-SAML协议](./认证源-CAS协议.md)
- [认证源-企业微信](./认证源-企业微信.md)
- [认证源-钉钉](./认证源-钉钉.md)
- [认证源-飞书](./认证源-飞书.md)
