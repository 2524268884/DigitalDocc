---
title: "单点登录-CAS协议"
description: "配置专属集成平台与第三方应用之间的 CAS 协议单点登录，实现简单高效的企业内部系统认证接入。"
---

## 概述

<ImageZoom
  src="/images/应用中心/应用单点登录/单点登录-CAS协议/overview.png"
  alt="单点登录-CAS协议概览截图"
  width={1200}
  height={720}
/>

CAS（Central Authentication Service，中央认证服务）是一种开放标准的单点登录协议，由耶鲁大学最初开发并维护，现由 Apereo 基金会管理。CAS 协议设计简单高效，是企业实现单点登录（SSO）最常用的协议之一。

### 核心概念

| 术语 | 说明 |
|------|------|
| **CAS 服务器** | 负责集中处理用户认证的服务器（相当于 IdP） |
| **CAS 客户端** | 需要进行身份验证的应用系统（相当于 SP） |
| **TGT** | Ticket Granting Ticket，用户登录 CAS 服务器后获得的票据，存储在 CAS 服务器的 Session 中 |
| **ST** | Service Ticket，CAS 服务器为特定服务生成的一次性票据，用于访问目标应用 |

### 协议版本

| 版本 | 特性 |
|------|------|
| CAS 1.0 | 最初的简单协议版本 |
| CAS 2.0 | 添加了代理认证等特性 |
| CAS 3.0 | 增加属性交换能力，验证响应可携带用户属性信息 |
| CAS 4.0/5.0 | 增强安全性和扩展性，支持 OAuth、SAML 等多种协议 |

### CAS 工作流程

<Steps>
<Step>
### 步骤 1：用户访问应用

<ImageZoom
  src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-01.png"
  alt="用户访问应用截图"
  width={1200}
  height={720}
/>

用户请求访问受保护的应用资源。
</Step>

<Step>
### 步骤 2：重定向到 CAS 服务器

<ImageZoom
  src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-02.png"
  alt="重定向到 CAS 服务器截图"
  width={1200}
  height={720}
/>

应用检测到用户未登录，将其重定向到 CAS 服务器。
</Step>

<Step>
### 步骤 3：用户认证

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-03.png" alt="用户认证截图" width="1200" height="720" */}

用户在 CAS 服务器提供身份凭证进行认证。
</Step>

<Step>
### 步骤 4：生成 ST 并重定向

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-04.png" alt="生成 ST 并重定向截图" width="1200" height="720" */}

认证成功后，CAS 服务器生成 ST，并将用户重定向回应用。
</Step>

<Step>
### 步骤 5：应用验证 ST

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-05.png" alt="应用验证 ST截图" width="1200" height="720" */}

应用通过后端渠道向 CAS 服务器验证 ST 的有效性。
</Step>

<Step>
### 步骤 6：授予访问权限

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-06.png" alt="授予访问权限截图" width="1200" height="720" */}

验证通过后，应用为用户提供访问权限。
</Step>
</Steps>

### CAS 优势

- **简单易用**：协议设计简洁，实现和集成较为容易
- **安全可靠**：票据是一次性的，减少重放攻击的风险
- **中心化认证**：所有认证都在 CAS 服务器进行，简化管理
- **丰富的客户端**：支持各种语言和框架的客户端库

## 前提条件

- 拥有专属集成平台管理员账号
- 目标应用支持 CAS 协议
- 确保专属集成平台中已创建相应用户

## 操作步骤

### 步骤 1：添加自定义应用

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-07.png" alt="添加自定义应用截图" width="1200" height="720" */}

<Steps>
<Step>
### 步骤 1：登录管理控制台

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-08.png" alt="登录管理控制台截图" width="1200" height="720" */}

登录专属集成平台管理控制台。
</Step>

<Step>
### 步骤 2：进入应用市场

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-09.png" alt="进入应用市场截图" width="1200" height="720" */}

导航至**应用管理 > 应用市场**。
</Step>

<Step>
### 步骤 3：添加应用

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-10.png" alt="添加应用截图" width="1200" height="720" */}

选择**添加应用 > 自定义应用**，填写基本信息。
</Step>
</Steps>

### 步骤 2：配置应用基本信息

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-11.png" alt="配置应用基本信息截图" width="1200" height="720" */}

| 参数 | 说明 |
|------|------|
| 自定义应用名称 | 自定义应用名称，建议使用有意义的描述性名称 |
| 应用分类 | 可选择已有分类或自定义新增分类 |
| 首页地址 | 应用的主页地址 |
| Client ID | 系统自动生成，不可编辑 |
| Client Secret | 系统自动生成，不可编辑，可重制 |

### 步骤 3：配置 CAS 协议参数

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-12.png" alt="配置 CAS 协议参数截图" width="1200" height="720" */}

在应用详情页面，切换到**登录配置**选项卡，配置以下参数：

| 参数 | 说明 |
|------|------|
| 本地登录方式 | 控制访问该应用的登录方式，可选用户名密码、短信验证码、邮箱验证码等 |
| 企业认证登录 | 该登录方式根据"企业认证源"中的认证方式进行登录 |
| 多因子认证 | 认证通过后触发短信或邮箱的二次验证 |
| 单点协议 | 选择 **CAS** 协议 |
| Login URL | 应用的登录回调地址，CAS 服务器认证成功后重定向的地址 |
| Logout URL | 应用的登出地址，用户注销时的处理地址 |
| Ver2.2 Validate URL | CAS 2.2 版本的验证 URL，用于验证 Service Ticket |
| Ver3.0 Validate URL | CAS 3.0 版本的验证 URL，用于验证 Service Ticket 并可返回用户属性 |
| Service URL | 目标应用提供的服务 URL，用于标识 CAS 客户端 |

### 步骤 4：在目标应用配置 CAS 客户端

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-13.png" alt="在目标应用配置 CAS 客户端截图" width="1200" height="720" */}

以宜搭应用为例：

1. 确认宜搭应用支持的 CAS 协议版本（2.2 或 3.0）
2. 在宜搭应用的单点登录配置中填写以下信息：
   - CAS 服务器地址：专属集成平台提供的 CAS 服务地址
   - Client ID：从专属集成平台获取的应用 Client ID
   - Client Secret：从专属集成平台获取的应用 Client Secret
   - 回调 URL：宜搭应用接收 CAS 票据的 URL
3. 获取宜搭应用的 Service URL，填写到专属集成平台的 CAS 配置中

### 步骤 5：配置授权范围

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-14.png" alt="配置授权范围截图" width="1200" height="720" */}

在专属集成平台中，切换到**授权范围**选项卡，选择授权方式：
- 基于组织架构和用户授权
- 基于角色授权（RBAC）
- 基于用户属性授权（ABAC）

### 步骤 6：测试验证

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-15.png" alt="测试验证截图" width="1200" height="720" */}

<Steps>
<Step>
### 步骤 1：访问应用

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-16.png" alt="访问应用截图" width="1200" height="720" */}

在浏览器中访问目标应用的 URL。
</Step>

<Step>
### 步骤 2：完成认证

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-17.png" alt="完成认证截图" width="1200" height="720" */}

如果用户未登录，应自动重定向到专属集成平台的登录页面，完成身份认证。
</Step>

<Step>
### 步骤 3：验证结果

{/* DOC_IMAGE src="/images/应用中心/应用单点登录/单点登录-CAS协议/step-18.png" alt="验证结果截图" width="1200" height="720" */}

认证成功后，应自动重定向回目标应用，并已处于登录状态。
</Step>
</Steps>

## 高级特性

### 代理认证

CAS 2.0 及以上版本支持代理认证，允许一个应用代表用户访问另一个应用：

1. **代理票据（Proxy Ticket）**：允许后端服务代表用户访问其他服务
2. **代理回调（Proxy Callback）**：验证代理请求的真实性
3. **代理链（Proxy Chain）**：控制哪些应用可以代理用户身份

### 属性交换

CAS 3.0 引入属性交换功能，使 CAS 服务器可以在票据验证响应中包含用户属性：

- **基本属性**：用户名、邮箱、电话等
- **角色信息**：用户在系统中的角色
- **自定义属性**：根据业务需求定义的特定属性

### 单点注销

CAS 提供单点注销功能，用户在一处注销后可以自动注销所有已登录的应用：

- **前端渠道注销**：通过浏览器重定向实现
- **后端渠道注销**：通过服务器间直接通信实现
- **注销请求回调**：通知各应用用户已注销

### 多种认证方式

CAS 服务器支持多种认证方式：
- 用户名/密码
- LDAP/Active Directory
- 数据库认证
- 多因子认证（短信、邮箱、OTP）
- OAuth/OpenID（社交登录）

## 常见问题排查

| 问题现象 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 无法重定向到 CAS 服务器 | Service URL 配置错误 | 检查目标应用中的 CAS 服务器地址配置 |
| 重定向回应用后登录失败 | Ticket 验证失败 | 确认验证 URL 正确，检查 Service URL 一致性 |
| 获取不到用户信息 | 协议版本不匹配 | 确保使用正确的 CAS 协议版本（2.2 或 3.0） |
| 多次重定向循环 | 客户端配置错误 | 检查 CAS 客户端配置，特别是 Cookie 设置 |
| 注销后其他应用仍保持登录 | 未实现全局注销 | 配置 CAS 的 Single Logout 功能 |

## CAS 与其他协议对比

### CAS vs SAML

| 特性 | CAS | SAML |
|------|-----|------|
| 复杂度 | 相对简单 | 较为复杂 |
| 安全性 | 基于票据的安全模型 | 基于断言的安全模型，支持签名和加密 |
| 标准化 | 较低，主要在高等教育领域流行 | 高度标准化，跨行业广泛采用 |
| 适用场景 | 内部系统集成，教育机构 | 企业级系统集成，跨组织联盟 |

### CAS vs OAuth/OIDC

| 特性 | CAS | OAuth/OIDC |
|------|-----|------------|
| 主要目的 | 身份认证 | OAuth 主要授权，OIDC 添加认证层 |
| 使用范围 | 企业内部应用 | Web/移动应用，API 访问控制 |
| 令牌类型 | 一次性票据 | 访问令牌、刷新令牌、ID 令牌 |
| 客户端支持 | 传统 Web 应用 | Web、移动、单页应用、物联网设备等 |

## 常见问题（FAQ）

<Accordions>
<Accordion title="CAS 与 OIDC 或 SAML 相比有什么优缺点？">
CAS 相对简单易用，适合企业内部系统集成，特别是在教育领域广泛应用。优点是实现简单、客户端库丰富；缺点是标准化程度低于 SAML/OIDC，跨组织集成能力较弱。
</Accordion>

<Accordion title="CAS 如何实现单点注销？">
CAS 支持两种单点注销方式：前端渠道注销（通过浏览器重定向）和后端渠道注销（服务器间直接通信）。配置 Single Logout 功能后，用户在一处注销会自动注销所有已登录的应用。
</Accordion>

<Accordion title="CAS 能否与现有用户目录集成？">
是的，CAS 服务器支持与 LDAP、Active Directory、数据库等多种用户存储系统集成，可以利用企业现有的用户目录。
</Accordion>

<Accordion title="如何处理 CAS 客户端与服务器的时钟同步问题？">
时钟不同步可能导致票据验证失败。建议使用 NTP 服务保持服务器时间同步，或适当增加票据有效期容错时间。
</Accordion>

<Accordion title="CAS 是否支持多租户架构？">
CAS 5.0 及以上版本支持多租户架构，可以为不同的租户提供独立的认证策略和界面。
</Accordion>
</Accordions>

## 实施最佳实践

**安全建议**
- 使用 HTTPS 保护所有 CAS 通信
- 设置适当的票据有效期（通常为 5-10 秒）
- 启用 CSRF 保护机制
- 实现 IP 地址过滤和异常登录检测
- 定期更换 Client Secret
- 配置适当的会话超时策略

**管理建议**
- 实施用户生命周期管理
- 建立应用注册和审核流程
- 定期审计登录日志
- 建立监控和告警机制
- 制定应急响应计划

## 相关文档

<Cards>
  <Card title="单点登录配置" href="/docs/应用中心/单点登录配置" />
  <Card title="单点登录-OIDC" href="/docs/应用中心/应用单点登录/单点登录-OIDC" />
  <Card title="单点登录-SAML 协议" href="/docs/应用中心/应用单点登录/单点登录-SAML协议" />
  <Card title="单点登出配置" href="/docs/应用中心/单点登出配置" />
</Cards>